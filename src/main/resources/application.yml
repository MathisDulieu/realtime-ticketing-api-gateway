spring:
  application:
    name: realtime-ticketing-api-gateway

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      probes:
        enabled: true

info:
  app:
    name: realtime-ticketing-api-gateway
    description: Public API gateway for the real-time ticketing platform